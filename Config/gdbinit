tui enable

layout src

focus cmd


define epiconf
    help epiconf
end

document epiconf
    list of command:
        dlistPrint   : print double linked list
        llistPrint   : print linked list
        intlistPrint : print n first element of a int list
        preload      : load arg as dynamic library (whithout '.so')
        libPath      : load dynamic library
end

define dlistPrint
  set var $n = $arg0->head
  while $n
    print $n->data
    set var $n = $n->next
  end
end

document dlistPrint
    tg
end

define llistPrint
    set var $n = $arg0
    while $n
        print $n->data
        set var $n = $n->next;
    end
end

define intlistPrint
    set var $i = $arg1
    while $i
        print $arg0[$i]
        set var $i = $i-1
    end
end

define preload
    set exec-wrapper env LD_PRELOAD=./$arg0.so
end

define libPath
    set env LD_LIBRARY_PATH=.
end
